sudo: required #tell travis taht we need super level permisison

services:
  - docker #copy of docker

before_install: #before test run or installing start, need to run few command before installation
  - docker build -t gulrezaktharkhan/docker-react -f Dockerfile.dev . 

script:
  - docker run gulrezaktharkhan/docker-react npm run test -- --coverage